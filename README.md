# Aedin-ctrl.github.io

My personal site, hosted at [aedinlai.com](https://aedinlai.com).

`docs/index.html` is self-contained (styles + script inline). A few fields
are still placeholders marked `[TODO ...]` in the projects, resume, and
contact panels &mdash; fill those in with real content and remove the
`proj-todo` class on those elements.
